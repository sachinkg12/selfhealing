package com.sachingupta.selfhealing.agent;

/** The five phases of the agent loop: detect, correlate, hypothesize, remediate, verify. */
public enum ReasoningPhase {
    DETECT,
    CORRELATE,
    HYPOTHESIZE,
    REMEDIATE,
    VERIFY
}
